<?php

namespace Route;

class RouteSuggestion
{
    public static function getRootSuggestion($root): \Route\RouteRoot
    {
        //Here Im trying to check the $root against the available array
        //and loop through and find the closest match, then return a route?? to
        //an exception that displays = fruit does not exist do you mean $closestMatch.
    }
}